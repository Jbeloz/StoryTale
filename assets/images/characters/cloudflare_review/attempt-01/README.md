# Cloudflare Character Review - Attempt 01

- `head.png` is the single Cloudflare-generated head attempt. The green
  background was removed locally; the character design was not regenerated or
  redrawn by another image model.
- `body.png` is the untouched blank body-shape reference.
- `full-body.png` is the untouched blank full-body proportion reference.

The body and full-body files remain blank until the head design is approved.
Create `attempt-02` only after the user asks for the next attempt.

Head generation used only two references:

1. strict head silhouette and viewing angle;
2. approved head colors and drawing style.

The prompt explicitly prohibited the neck, shoulders, torso, clothing, hands,
text, props, and shadows.
