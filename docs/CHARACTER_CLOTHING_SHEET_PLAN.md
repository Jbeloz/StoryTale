# Character Clothing Sheet Plan (Superseded)

The clothing-only plan was replaced by the broader
[Character Sheet V1 Implementation Plan](CHARACTER_SHEET_PLAN.md).

Use `CHARACTER_SHEET_PLAN.md` as the only Phase 7G.1B authority. It defines the
approved `4096 x 4096` native-size separated head, face, hair, torso, and limb layout; the
local full-body proof; fixed crop/mask processing; and the current
implementation order.
